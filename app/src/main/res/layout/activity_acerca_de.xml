<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#1a1a2e"
    android:padding="32dp">

    <!-- Logo -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/logo_instamalaga"
        android:scaleType="centerCrop"
        android:layout_marginBottom="24dp"/>

    <!-- Nombre de la app -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="InstaMálaga"
        android:textColor="#ffffff"
        android:textSize="28sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <!-- Versión -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Versión 1.0"
        android:textColor="#e94560"
        android:textSize="14sp"
        android:layout_marginBottom="32dp"/>

    <!-- Descripción -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="InstaMálaga es una galería fotográfica que muestra los rincones más bonitos de Málaga y su provincia.
Proyecto creado para la asignatura PMDM"
        android:textColor="#aaaaaa"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- Autor -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Desarrollado por:"
        android:textColor="#aaaaaa"
        android:textSize="12sp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fran Rebollo"
        android:textColor="#ffffff"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="DAM · PMDM · 2025-2026"
        android:textColor="#aaaaaa"
        android:textSize="12sp"
        android:layout_marginBottom="40dp"/>

    <!-- Botón volver -->
    <Button
        android:id="@+id/btnVolverMain"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="← Volver"
        android:backgroundTint="#e94560"
        android:textColor="#ffffff"/>

</LinearLayout>